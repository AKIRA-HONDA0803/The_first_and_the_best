<div class='row'>
   <div class="teacher-list-box col-4 offset-lg-2">
<%  @user.each do |user| %>
<div class="card px-0 shadow-lg col-3 style="width: 18rem; ">
   <%= link_to user_path(user.id) do%>
   <%= attachment_image_tag user, :profile_image,:fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg",class:"card-img-top"; %>
   <% end %>
   <div class="card-body">
      <div class="row">
     <%= user.name %>
  </div>
  </div>
  </div>



<% end %>
</div>
</div>
